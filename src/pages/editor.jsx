import React from 'react';
import { EditorComponent, Layout } from '@/components';

const EditorPage = () => {
  return (
    <Layout>
      <EditorComponent />
    </Layout>
  );
};

export default EditorPage;
